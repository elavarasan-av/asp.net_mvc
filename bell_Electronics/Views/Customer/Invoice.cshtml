@model IEnumerable<bell_Electronics.Models.Invoice>
@{
    ViewBag.Title = "Invoice";
    Layout = "~/Views/Shared/_LayoutDefault.cshtml";
}

@*@using (Html.BeginForm("Invoice",
                        "Customer",
                        FormMethod.Post,
                        new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()*@


    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h2 class="m-0 font-weight-bold text-primary" style="text-align:center">Order Invoice</h2>
        </div>
        <div class="card-body">
            <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                <tr>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.OrderNo)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.ProductsName)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.Price)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.Quantity)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.TotalPrice)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.OrderDate)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.Status)
                    </th>

                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center">
                            @Html.DisplayFor(modelItem => item.OrderNo)
                        </td>
                        <td class="text-center">
                            @Html.DisplayFor(modelItem => item.ProductsName)
                        </td>
                        <td class="text-center">
                           ₹ @Html.DisplayFor(modelItem => item.Price)
                        </td>
                        <td class="text-center">
                            @Html.DisplayFor(modelItem => item.Quantity)
                        </td>
                        <td class="text-center">
                           ₹ @Html.DisplayFor(modelItem => item.TotalPrice)
                        </td>
                        <td class="text-center">
                            @Html.DisplayFor(modelItem => item.OrderDate)
                        </td>
                        <td class="text-center">
                            @Html.DisplayFor(modelItem => item.Status)
                        </td>
                    </tr>
                }
                <tr>
                    <td colspan="3"></td>
                    <td> Amount :   </td>
                    <td>₹ @Session["Amount"]</td>
                </tr>
            </table>
        </div>
        <div class="card-footer">
           <a href="~//Customer/Pdf" class="btn btn-info">confirom</a>
        </div>
    </div>
@*}*@

